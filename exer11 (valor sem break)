sn = 0
qn = 0

nota=0
while nota != -1:
    nota = int(input("Digite sue valor(-1 para para): "))

    if nota != -1:
        sn += nota
        qn += 1

    elif qn >0:
        media = sn/qn
        print("A media dos valores é:",media)
    else:
        print("Nenhuma valor enserida")
